const fastName = 'Himaloy';
const lastName = 'Himu';
const fullName = fastName + " " + lastName + " is a bad boy in this world";

const fullName = `${firstName} ${50+50+50} is a good boy,`;
console.log(fullName);
